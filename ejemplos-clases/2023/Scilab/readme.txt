Para que el zcos ejecute el fdexu2.sci hay que hacer desde el command line:

exec('fdexu2.sci')

En el bloque del XCOS hay que darle OK para que aparezca la chance de editar 
el llamadao a la función.

Toda moficación a un .sci hay que darle exec('') otra vez.
